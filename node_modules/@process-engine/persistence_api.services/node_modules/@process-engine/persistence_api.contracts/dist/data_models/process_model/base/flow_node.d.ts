import { BpmnType } from '../constants';
import { BaseElement } from './base_element';
/**
 * Base class for all FlowNodes.
 * These include Events, Gateways and Activities.
 */
export declare abstract class FlowNode extends BaseElement {
    name: string;
    incoming: Array<string>;
    outgoing: Array<string>;
    defaultOutgoingSequenceFlowId?: string;
    abstract readonly bpmnType: BpmnType;
}
