import { IContainer } from 'addict-ioc';
import { Model } from '@process-engine/persistence_api.contracts';
import { IFlowNodeHandler, IFlowNodeHandlerDedicatedFactory } from '@process-engine/process_engine_contracts';
export declare class IntermediateThrowEventFactory implements IFlowNodeHandlerDedicatedFactory<Model.Events.IntermediateThrowEvent> {
    private container;
    constructor(container: IContainer);
    create(flowNode: Model.Events.IntermediateThrowEvent): Promise<IFlowNodeHandler<Model.Events.IntermediateThrowEvent>>;
}
