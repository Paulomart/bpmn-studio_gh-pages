import { IContainer } from 'addict-ioc';
import { Model } from '@process-engine/persistence_api.contracts';
import { IBoundaryEventHandler, IBoundaryEventHandlerFactory } from '@process-engine/process_engine_contracts';
export declare class BoundaryEventHandlerFactory implements IBoundaryEventHandlerFactory {
    private container;
    constructor(container: IContainer);
    create(boundaryEventNode: Model.Events.BoundaryEvent): Promise<IBoundaryEventHandler>;
    private resolveHandlerInstance;
    private getEventDefinitionType;
}
