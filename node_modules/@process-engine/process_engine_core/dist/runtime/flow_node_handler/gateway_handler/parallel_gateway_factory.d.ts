import { IContainer } from 'addict-ioc';
import { Model, ProcessToken } from '@process-engine/persistence_api.contracts';
import { IFlowNodeHandler, IFlowNodeHandlerDedicatedFactory } from '@process-engine/process_engine_contracts';
export declare class ParallelGatewayFactory implements IFlowNodeHandlerDedicatedFactory<Model.Gateways.ParallelGateway> {
    private container;
    constructor(container: IContainer);
    create(flowNode: Model.Gateways.ParallelGateway, processToken: ProcessToken): Promise<IFlowNodeHandler<Model.Gateways.ParallelGateway>>;
}
