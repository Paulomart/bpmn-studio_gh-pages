import { BaseRouter } from '@essential-projects/http_node';
import { SwaggerController } from './swagger_controller';
export declare class SwaggerRouter extends BaseRouter {
    private swaggerController;
    constructor(swaggerController: SwaggerController);
    readonly baseRoute: string;
    initializeRouter(): Promise<void>;
    private registerRoutes;
}
