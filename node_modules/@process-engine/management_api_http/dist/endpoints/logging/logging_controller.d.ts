import { HttpRequestWithIdentity } from '@essential-projects/http_contracts';
import { APIs, HttpController } from '@process-engine/management_api_contracts';
import { Response } from 'express';
export declare class LoggingController implements HttpController.ILoggingHttpController {
    private httpCodeSuccessfulResponse;
    private loggingService;
    constructor(loggingService: APIs.ILoggingManagementApi);
    getProcessModelLog(request: HttpRequestWithIdentity, response: Response): Promise<void>;
    getProcessInstanceLog(request: HttpRequestWithIdentity, response: Response): Promise<void>;
}
