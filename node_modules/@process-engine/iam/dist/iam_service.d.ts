import { IHttpClient } from '@essential-projects/http_contracts';
import { IIAMService, IIdentity } from '@essential-projects/iam_contracts';
export declare class IAMService implements IIAMService {
    private httpClient;
    private config;
    private cache;
    private httpResponseUnauthorizedCode;
    private httpResponseForbiddenCode;
    private httpResponseOkNoContentCode;
    constructor(httpClient: IHttpClient);
    initialize(): Promise<void>;
    ensureHasClaim(identity: IIdentity, claimName: string, claimValue?: string): Promise<void>;
    private checkIfTokenIsInternalToken;
    private checkIfTokenIsDummyToken;
    private checkIfUserHasClaim;
    private getFromCache;
    private getFromAuthority;
}
