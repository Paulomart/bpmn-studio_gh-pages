import { BpmnType } from '../constants';
import { Gateway } from './gateway';
/**
 * Describes an InclusiveGateway.
 *
 * Currently not evaluated during runtime.
 */
export declare class InclusiveGateway extends Gateway {
    get bpmnType(): BpmnType;
}
