import { BaseElement } from '../base/index';
/**
 * Describes a global BPMN error element.
 * Can be thrown by an ErrorEndEvent and caught by an ErrorBoundaryEvent.
 */
export declare class Error extends BaseElement {
    name?: string;
    code?: string;
    message?: string;
}
