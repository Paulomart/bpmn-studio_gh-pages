import { InvocationContainer } from 'addict-ioc';
declare type startupArgs = {
    sqlitePath?: string;
    logFilePath?: string;
    container?: InvocationContainer;
    minimalSetup?: boolean;
    workDir?: string;
    enableHttp?: boolean;
    useHttpRootRoutes?: boolean;
};
export declare function startRuntime(args?: startupArgs | string): Promise<void>;
export {};
