export declare type ParsedVersion = {
    baseString: string;
    releaseChannelName: string;
    releaseChannelNumber?: number;
};
export declare function parseVersion(version: string): ParsedVersion;
