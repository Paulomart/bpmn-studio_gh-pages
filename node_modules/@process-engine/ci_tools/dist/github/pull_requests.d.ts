export declare type PullRequest = {
    number: number;
    title: string;
    closedIssueNumbers: number[];
    headSha: string;
    mergeCommitSha: string;
    mergedAt: string;
};
export declare function getMergedPullRequests(since: string): Promise<PullRequest[]>;
