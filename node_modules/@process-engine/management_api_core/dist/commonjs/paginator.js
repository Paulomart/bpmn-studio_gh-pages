"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const loggerhythm_1 = require("loggerhythm");
const logger = loggerhythm_1.Logger.createLogger('processengine:management_api_core:paginator');
function applyPagination(values, offset, limit) {
    if (offset > values.length) {
        logger.warn(`The offset of ${offset} is larger than the given value list (${values.length})! Returning an empty result set.`);
        return [];
    }
    let valueSubset = offset > 0
        ? values.slice(offset)
        : values;
    const limitIsOutOfValueListBounds = limit < 1 || limit >= valueSubset.length;
    if (limitIsOutOfValueListBounds) {
        return valueSubset;
    }
    valueSubset = valueSubset.slice(0, limit);
    return valueSubset;
}
exports.applyPagination = applyPagination;
//# sourceMappingURL=paginator.js.map