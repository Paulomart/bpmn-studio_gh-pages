import { BaseEventMessage } from '../base_event_message';
import { BpmnType } from '../../data_models/bpmn_type';
/**
 * The message sent when an Activity has been reached.
 */
export declare class ActivityReachedMessage extends BaseEventMessage {
    flowNodeType: BpmnType;
    constructor(correlationId: string, processModelId: string, processInstanceId: string, flowNodeId: string, flowNodeInstanceId: string, flowNodeType: BpmnType, currentToken: any);
}
