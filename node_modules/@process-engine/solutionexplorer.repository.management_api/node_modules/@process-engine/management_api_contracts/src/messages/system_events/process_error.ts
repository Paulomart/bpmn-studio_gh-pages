import {BaseEventMessage} from '../base_event_message';

/**
 * The message which is send, when a new ProcessInstance runs into an error.
 */
export class ProcessErrorMessage extends BaseEventMessage {}
