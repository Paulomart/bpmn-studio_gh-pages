import {BaseEventMessage} from '../base_event_message';

/**
 * Represents the message which is send, when a ManualTask has been reached.
 */
export class ManualTaskReachedMessage extends BaseEventMessage {}
