import { Subscription } from '@essential-projects/event_aggregator_contracts';
/**
 * The base class for definining cronjob messages.
 */
export declare class CronjobBaseEvent {
    readonly subscription: Subscription;
    readonly startEventId: string;
    readonly cronjob: string;
    constructor(subscription: Subscription, startEventId: string, cronjob: string);
}
export declare class CronjobBaseEventMessage {
    readonly processModelId: string;
    readonly cronjobs: Array<CronjobBaseEvent>;
    constructor(processModelId: string, cronjobs: Array<CronjobBaseEvent>);
}
