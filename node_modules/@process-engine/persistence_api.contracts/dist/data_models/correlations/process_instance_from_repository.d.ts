import { IIdentity } from '@essential-projects/iam_contracts';
import { CorrelationState } from './correlation_state';
/**
 * Describes a ProcessInstance as it is stored in the database.
 */
export declare class ProcessInstanceFromRepository {
    correlationId: string;
    processModelHash: string;
    processModelId: string;
    processInstanceId: string;
    parentProcessInstanceId?: string;
    state: CorrelationState;
    error: Error;
    identity: IIdentity;
    createdAt: Date;
    updatedAt: Date;
    finishedAt?: Date;
    terminatedBy?: IIdentity;
}
