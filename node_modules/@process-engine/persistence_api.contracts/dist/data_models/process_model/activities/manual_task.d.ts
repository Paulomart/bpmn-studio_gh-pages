import { BpmnType } from '../constants';
import { Activity } from './activity';
/**
 * Describes a BPMN ManualTask.
 *
 * Currently not evaluated during runtime.
 */
export declare class ManualTask extends Activity {
    get bpmnType(): BpmnType;
}
