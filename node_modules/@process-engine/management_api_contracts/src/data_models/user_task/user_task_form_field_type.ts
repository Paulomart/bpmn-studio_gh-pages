/**
 * Determines the type of a UserTasks FormField.
 */
export enum UserTaskFormFieldType {
  boolean = 'boolean',
  date = 'date',
  enum = 'enum',
  long = 'long',
  number = 'number',
  string = 'string',
}
