import { ExtensionBootstrapper } from '@essential-projects/bootstrapper';
import { Container, IFactoryAsync, IInstanceWrapper } from 'addict-ioc';
export declare class AppBootstrapper {
    readonly appRoot: string;
    readonly env: string;
    readonly configPath: string;
    protected readonly container: Container<IInstanceWrapper<any>>;
    private extensionBootstrapperFactory;
    private extensionBootstrapper;
    constructor(container: Container<IInstanceWrapper<any>>, extensionBootstrapperFactory: IFactoryAsync<ExtensionBootstrapper>, appRoot?: string);
    private initializeConfigProvider;
    initialize(): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
}
