import { IHttpRouter } from '@essential-projects/http_contracts';
import * as express from 'express';
export declare abstract class BaseRouter implements IHttpRouter {
    config: any;
    private _router;
    readonly router: express.Router;
    readonly baseRoute: string;
    initialize(): Promise<any> | any;
    abstract initializeRouter(): Promise<any> | any;
    /**
     * Inheriting routers can override this method to cleanup on shutdown.
     */
    dispose(): Promise<void> | void;
    protected invokeAsPromiseIfPossible(functionToInvoke: any, invocationContext: any, invocationParameter?: Array<any>): Promise<any>;
}
