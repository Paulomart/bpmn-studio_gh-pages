/// <reference types="socket.io" />
import { IHttpSocketEndpoint } from '@essential-projects/http_contracts';
export declare abstract class BaseSocketEndpoint implements IHttpSocketEndpoint {
    readonly namespace: string;
    abstract initializeEndpoint(socketIo: SocketIO.Namespace): Promise<any> | any;
    /**
     * If any resources need to be disposed when the serves closes down, this
     * method can be implemented in the inheriting class.
     */
    dispose(): Promise<void> | void;
}
