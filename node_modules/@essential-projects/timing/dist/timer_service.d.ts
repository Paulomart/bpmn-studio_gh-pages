import * as moment from 'moment';
import { IEventAggregator } from '@essential-projects/event_aggregator_contracts';
import { ITimerService } from '@essential-projects/timing_contracts';
export declare class TimerService implements ITimerService {
    private jobs;
    private readonly eventAggregator;
    constructor(eventAggregator: IEventAggregator);
    cancel(timerId: string): void;
    oneShot(date: moment.Moment, eventName: string): string;
    cronjob(crontab: string, eventName: string): string;
    private createTimer;
    private ensureTimerIsValid;
    private validateOneShotTimer;
    private validatePeriodicTimer;
    private createJob;
    private timerElapsed;
    private getJob;
    private cacheJob;
    private removeJob;
}
