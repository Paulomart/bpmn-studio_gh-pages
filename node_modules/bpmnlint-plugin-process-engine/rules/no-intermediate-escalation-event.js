"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lintUtils = require("bpmnlint-utils");
/**
 * Rule that reports if the process contains a escalation intermediate Event.
 *
 * Intermediate escalation Events are currently not supported by the ProcessEngine.
 */
module.exports = () => {
    function check(node, reporter) {
        const nodeIsIntermediateEvent = lintUtils.is(node, 'bpmn:IntermediateEvent');
        if (nodeIsIntermediateEvent) {
            const intermediateEventHasEventDefinitions = node.eventDefinitions !== undefined;
            if (intermediateEventHasEventDefinitions) {
                const intermediateEventIsEscalationEvent = node.eventDefinitions.some((definition) => {
                    return lintUtils.is(definition, 'bpmn:EscalationEventDefinition');
                });
                if (intermediateEventIsEscalationEvent) {
                    reporter.report(node.id, 'Intemediate escalation Events are currently not supported!');
                }
            }
        }
    }
    return {
        check: check,
    };
};
//# sourceMappingURL=no-intermediate-escalation-event.js.map