"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lintUtils = require("bpmnlint-utils");
/**
 * Rule that reports if the process contains a intermediate conditional ThrowEvent.
 *
 * Intermediate conditional ThrowEvent are currently not supported by the ProcessEngine.
 */
module.exports = () => {
    function check(node, reporter) {
        const nodeIsIntermediateThrowEvent = lintUtils.is(node, 'bpmn:IntermediateThrowEvent');
        if (nodeIsIntermediateThrowEvent) {
            const intermediateEventHasEventDefinitions = node.eventDefinitions !== undefined;
            if (intermediateEventHasEventDefinitions) {
                const intermediateEventIsConditionalEvent = node.eventDefinitions.some((definition) => {
                    return lintUtils.is(definition, 'bpmn:ConditionalEventDefinition');
                });
                if (intermediateEventIsConditionalEvent) {
                    reporter.report(node.id, 'Intemediate conditional ThrowEvents are currently not supported!');
                }
            }
        }
    }
    return {
        check: check,
    };
};
//# sourceMappingURL=no-intermediate-conditional-throw-event.js.map